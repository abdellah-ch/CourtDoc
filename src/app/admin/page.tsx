'use client';
const Page = () => {
    console.log(process.env.JWT_SECRET);

    return (
        <div className="relative w-full h-screen">
            test
        </div>
    );
}

export default Page;
